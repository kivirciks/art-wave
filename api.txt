1) GET:	/api/v1/models
Получение id модели (присоединяется к каждому запросу генерации)
ResponseBody:
4 # Просто id

2) POST:/api/v1/generate
RequestBody:
{
    "modelId" : 4, 	 # id модели из 1, 
    "params" : {
        "numImages" : 4, # Количество генерируемых изображений
        "generateParams": {
            "query":"котики" # Сам запрос
        },
        "width" : 1024,	 # Ширина изображения для генерации
        "height" : 1024, # Высота изображения для генерации
	"negativePromptUnclip": "текст"	# Негативный промпт
    }
}
ResponseBody:
bad29dfc-a619-4f3e-87ac-d302820da357 # Просто строка с uuid

3) POST:/api/v1/check_generation
RequestBody:
{
    "uuid" : "bad29dfc-a619-4f3e-87ac-d302820da357" # Строка с uuid для проверки генерации
}
ResponseBody:
Строка в виде ['байты_изображения_в_кодировке_base64']
! Если ошибка - это нормально, значит изображение ещё не сгенерилось, я не помню
  как обрабатывать конкретные ошибки в питоне))

4) GET: /api/v1/super_resolution
RequestBody:
{
    "image" : строка в виде ['байты_изображения_в_кодировке_base64']
    "resolution" : 4 # Размер увеличения, пока доступно 4 и 16, фиксим
}
ResponseBody:
{
    "image" : строка в виде ['байты_изображения_в_кодировке_base64']
}